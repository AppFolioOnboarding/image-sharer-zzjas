<h1>New Images#index</h1>

<%= simple_form_for @image do |f| %>
    <%= f.input :url, input_html: { value: @image.url.presence || '' } %>
    <%= f.input :tag_list, input_html: { value: f.object.tag_list.to_s} %>
    <%= f.submit "Create" %>
<% end %>
